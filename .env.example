# Supabase
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Data backend for src/data: "supabase" to use the live tables, anything else = in-memory fixtures.
# Leave unset during development so the app runs on fixtures without a live session.
EXPO_PUBLIC_DATA_BACKEND=
