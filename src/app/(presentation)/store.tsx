// Route wrapper — the Shop screen lives in src/shop (colocated with the Inventory overlay so the two catalogue surfaces share their components and styling).
export { default } from "@/src/shop/StoreScreen";
