// Route wrapper — the Inventory screen lives in src/shop (colocated with the Shop so the two catalogue surfaces share their components and styling).
export { default } from "@/src/shop/InventoryScreen";
